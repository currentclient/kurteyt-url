{
  "name": "kurteyt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy --verbose --force --stage $1",
    "package": "serverless package --verbose --force --stage $1",
    "remove": "serverless remove --verbose --force --stage $1",
    "up": "docker-compose up --build --remove-orphan",
    "box": "docker exec -it kurteyt-url_api_1 /bin/bash",
    "local:offline": "serverless offline",
    "local": "uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gitmoji-cli": "^3.2.12",
    "serverless": "~2.4.0",
    "serverless-dotenv-plugin": "^3.8.0",
    "serverless-iam-roles-per-function": "^2.0.2",
    "serverless-offline": "^6.5.0",
    "serverless-pseudo-parameters": "^2.5.0",
    "serverless-python-requirements": "^5.1.0"
  }
}
